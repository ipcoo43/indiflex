# A Tale of Two Sequences ( 두 시퀀스의 이야기 )

# 리스트에 내장된 함수들을 보면
lst = list()
# 대분분의 값을 변경할 수 있다.
print(dir(lst))
# append, count, extend, index, insert, pop, remove, reverse, sort

# 튜플에 내장된 함수들을 보면
tpl = tuple()
# count, index만 작동한다.
print(dir(tpl))
# count, index